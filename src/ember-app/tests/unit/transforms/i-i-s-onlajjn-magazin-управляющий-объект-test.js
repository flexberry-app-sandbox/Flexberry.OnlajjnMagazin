import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('transform:i-i-s-onlajjn-magazin-управляющий-объект', 'Unit | Transform | i i s onlajjn magazin управляющий объект', function(hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function(assert) {
    let transform = this.owner.lookup('transform:i-i-s-onlajjn-magazin-управляющий-объект');
    assert.ok(transform);
  });
});
